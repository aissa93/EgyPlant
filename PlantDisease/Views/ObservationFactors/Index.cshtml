@model IEnumerable<PlantDisease.Models.ObservationFactor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FactorValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Factor.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observation.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FactorValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Factor.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observation.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
